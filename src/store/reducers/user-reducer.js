import CREATE_USER from '../actions'

export default function userReducer(state,action){
    switch (action.type) {
        case :
            
            return;
            case :
            
            return;
            case :
            
            return;
            case :
            
            return;
    
        default:
            return state;
    }
}